import {ROUTING_PAGE1,ROUTING_PAGE2} from '../constantTypes.js';

export const pageRouting1Action=()=>{
	return({
		type: ROUTING_PAGE1
	})
}

export const pageRouting2Action=()=>{
	return({
		type: ROUTING_PAGE2
	})
}